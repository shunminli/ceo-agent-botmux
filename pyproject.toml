[project]
name = "ceo-agent-botmux"
version = "0.1.0"
description = "BotMux-style local agent team prototypes for one-person-company workflows."
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
